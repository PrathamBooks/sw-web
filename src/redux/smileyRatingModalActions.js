import { createActions } from "redux-actions";

export const { addSmileyRatingModal } = createActions('ADD_SMILEY_RATING_MODAL');
export const { removeSmileyRatingModal } = createActions('REMOVE_SMILEY_RATING_MODAL');
